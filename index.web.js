import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('SocialConnect', () => App);
AppRegistry.runApplication('SocialConnect', {
  rootTag: document.getElementById('root'),
});
